import styled from "styled-components";

const Container = styled.div`
  border: var(--border-container) solid #bc6bff;
  border-radius: 10px;
  padding: 1.5rem;
  margin: 3rem 5rem;
`;

export default Container;
